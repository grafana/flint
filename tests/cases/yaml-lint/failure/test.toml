[expected]
args = "run --full yaml-lint"
exit = 1
stderr = '''
[yaml-lint]
config.yml:2: bad spacing

flint: 1 check failed (yaml-lint)
💡 Try `flint run --fix` to auto-fix lint issues, then re-run `flint run` to verify.
'''

[fake_bins]
yaml-lint = '''
#!/bin/sh
printf '%s\n' 'config.yml:2: bad spacing'
exit 1
'''
