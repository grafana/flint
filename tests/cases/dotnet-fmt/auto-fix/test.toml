[expected]
args = "run --full --fix dotnet-fmt"
exit = 1
stderr = '''
flint: fixed: dotnet-fmt — commit before pushing
'''

[expected.files]
"Program.cs" = """
class Program
{
    static void Main()
    {
        System.Console.WriteLine("Hello");
    }
}
"""

[env]
DOTNET_CLI_HOME = "/tmp/dotnet-home"
DOTNET_NOLOGO = "1"
