[expected]
args = "run --full zizmor"
exit = 0

[env]
FLINT_CONFIG_DIR = ".github/config"
# Tests intentionally omit GitHub credentials; make zizmor's offline mode explicit
# so fixture output stays deterministic and does not include an implicit-mode warning.
ZIZMOR_OFFLINE = "true"
