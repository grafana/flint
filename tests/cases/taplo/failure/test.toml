[expected]
args = "run --full taplo"
exit = 1
stderr = '''
[taplo]
ERROR taplo:format_files: the file is not properly formatted path="<REPO>/data.toml"
ERROR operation failed error=some files were not properly formatted

flint: 1 check failed (taplo)
💡 Try `flint run --fix` to auto-fix lint issues, then re-run `flint run` to verify.
'''
