[expected]
args = "run --full ryl"
exit = 1
stderr = '''
[ryl]
<REPO>/config.yml
  2:1       error    syntax error: while parsing a node, did not find expected node content (syntax)


flint: 1 check failed (ryl)
💡 Try `flint run --fix` to auto-fix lint issues, then re-run `flint run` to verify.
'''

[env]
FLINT_CONFIG_DIR = ".github/config"
