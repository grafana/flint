[expected]
args = "run --fix flint-setup"
exit = 1
stdout = "  replaced \"github:koalaman/shellcheck\" → \"shellcheck\"\n  replaced \"pipx:ruff\" → \"ruff\"\n  normalized [tools] in <REPO>/mise.toml\n"
stderr = "flint: fixed: flint-setup — commit before pushing\n"

[expected.files]
"mise.toml" = '''
[tools]

# Linters
ruff = "0.15.0"
shellcheck = "v0.11.0"
'''
"flint.toml" = '''
[settings]
'''
