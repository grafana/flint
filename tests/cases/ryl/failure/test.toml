[expected]
args = "run --full ryl"
exit = 1
stderr = '''
[ryl]
<REPO>/config.yml
  1:6       error    syntax error: unclosed bracket '[' (syntax)


flint: 1 check failed (ryl)
💡 Try `flint run --fix` to auto-fix lint issues, then re-run `flint run` to verify.
'''

[env]
FLINT_CONFIG_DIR = ".github/config"
