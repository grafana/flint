[expected]
args = "run --full gofmt"
exit = 1
stderr = '''
[gofmt]
diff <REPO>/main.go.orig <REPO>/main.go
--- <REPO>/main.go.orig
+++ <REPO>/main.go
@@ -1,5 +1,5 @@
 package main
 
-func add(x int,y int) int {
-return x+y
+func add(x int, y int) int {
+	return x + y
 }

flint: 1 check failed (gofmt)
💡 Try `flint run --fix` to auto-fix lint issues, then re-run `flint run` to verify.
'''