[expected]
args = "--full --fix shellcheck actionlint"
exit = 1
stderr = """
[actionlint]
.github/workflows/ci.yml:6:23: undefined variable "foo". available variables are "env", "github", "inputs", "job", "matrix", "needs", "runner", "secrets", "steps", "strategy", "vars" [expression]
  |
6 |       - run: echo ${{ foo.bar }}
  |                       ^~~~~~~
[shellcheck]

In <REPO>/bad.sh line 2:
echo $1
     ^-- SC2086 (info): Double quote to prevent globbing and word splitting.

Did you mean:
echo "$1"

For more information:
  https://www.shellcheck.net/wiki/SC2086 -- Double quote to prevent globbing ...
flint: review: actionlint, shellcheck
"""