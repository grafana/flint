[expected]
args = "run --full taplo"
exit = 1
stderr = '''
[taplo]
ERROR the file is not properly formatted path="<REPO>/data.toml"

flint: 1 check failed (taplo)
💡 Try `flint run --fix` to auto-fix lint issues, then re-run `flint run` to verify.
'''
