[expected]
args = "run cargo-fmt"
exit = 1
stderr = '''
flint: unsupported cargo-fmt config file found: rustfmt.toml
  Flint only supports FLINT_CONFIG_DIR/rustfmt.toml for cargo-fmt. Move the config to the supported location or remove the alternate file.
'''
